import React from 'react';

import './Select.scss'

const Select = () => {
    return (
        <div>
            <div className="container"></div>
            <select>
                <option />
            </select>
        </div>
    );
};

export default Select;